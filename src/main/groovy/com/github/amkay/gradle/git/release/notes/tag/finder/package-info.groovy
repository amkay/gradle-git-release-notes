/**
 * Provides the strategies for finding the <em>Git tag</em> of the latest release version of the project to which the
 * plugin is applied.
 *
 * <p>
 *     The entry point to this package is the interface {@link TagFinder}.
 * </p>
 *
 * @author Max Käufer
 */
package com.github.amkay.gradle.git.release.notes.tag.finder;